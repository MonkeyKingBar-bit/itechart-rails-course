<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class:"form-control" %>
  </div>
  <br />
  <div class="field form-group">
    <%= form.select :transaction_type, ['Debit', 'Credit'], {}, class:"form-control"%>
  </div>
  <br />
  <div class="field form-group">
    <%= form.label :person %>
    <%= form.collection_select(:person_ids, @people, :id, :first_name, {}, { multiple: true, class: "form-control", required: true}) %>
  </div>
<!--  <br />-->
<!--  <div class="field form-group">-->
    <%#= form.number_field :user_id, id: :category_id, class:"form-control", value: current_user.id, type: "hidden" %>
<!--  </div>-->
  <br />
  <div class="actions">
    <%= form.submit class:'btn btn-secondary' %>
  </div>
<% end %>
